import { PapiClient } from '../papi-client';
import { SyncBody, SyncResponse, SyncJobInfo, SyncData } from '../entities';
export declare class SyncEndpoint {
    private service;
    constructor(service: PapiClient);
    post(body: SyncBody): Promise<SyncResponse>;
    jobInfo(uuid: string): Promise<SyncJobInfo>;
    data(uuid: string): Promise<SyncData>;
}
