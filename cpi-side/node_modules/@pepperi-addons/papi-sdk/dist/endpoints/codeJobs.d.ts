import { CodeJob } from '../entities/codeJobs';
import Endpoint from '../endpoint';
import { PapiClient } from '../papi-client';
declare class CodeJobEndpoint {
    private service;
    private uuid;
    private async;
    constructor(service: PapiClient, uuid: string, async: boolean);
    find(includeDeleted?: boolean): Promise<CodeJob>;
    get(includeDeleted?: boolean): Promise<CodeJob>;
    publish(body: {
        comment: string;
    }): Promise<any>;
    execute(): Promise<any>;
}
export declare class CodeJobsEndpoint extends Endpoint<CodeJob> {
    private isAsync;
    constructor(service: PapiClient);
    uuid(uuid: string): CodeJobEndpoint;
    async(): this;
}
export {};
