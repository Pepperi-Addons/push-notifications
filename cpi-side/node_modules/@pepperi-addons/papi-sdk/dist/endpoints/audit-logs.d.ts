import { AuditLog, AuditLogLines } from '../entities';
import Endpoint from '../endpoint';
import { PapiClient } from '../papi-client';
declare class AuditLogEndpoint {
    private service;
    private uuid;
    constructor(service: PapiClient, uuid: string);
    get(): Promise<any>;
    lines: Endpoint<AuditLogLines>;
}
export declare class AuditLogsEndpoint extends Endpoint<AuditLog> {
    constructor(service: PapiClient);
    uuid(uuid: string): AuditLogEndpoint;
}
export {};
