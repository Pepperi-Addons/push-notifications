import { PapiClient } from '../papi-client';
import { PNSMessage, PublishMessageResult, Subscription } from '../entities/subscription';
import Endpoint from '../endpoint';
export declare class NotificationEndpoint {
    private service;
    constructor(service: PapiClient);
    subscriptions: Endpoint<Subscription>;
    publish(body: PNSMessage): Promise<PublishMessageResult>;
}
