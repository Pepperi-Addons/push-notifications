import { CPIDBObject } from './db-object';
export declare class Workflow {
    private dbObject;
    constructor(dbObject: CPIDBObject);
    performTransition(actionId: string, actionResult: boolean): Promise<any>;
    startProgram(name: any): Promise<any>;
}
