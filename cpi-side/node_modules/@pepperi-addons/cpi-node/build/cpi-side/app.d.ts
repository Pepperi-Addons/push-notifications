import { AddonLoader } from './addon-loader';
import { AppEventsEmitter } from './events';
export declare class App {
    eventEmitter: AppEventsEmitter;
    addonLoader: AddonLoader;
    private wApp;
    constructor();
    start(): Promise<boolean>;
    reload(): Promise<void>;
    loadConfigurations(): Promise<void>;
    setupEventsHandler(): void;
    setupAddonApi(): Promise<void>;
    emitEventHandler(data: string, cb: (x: string) => void): void;
    addonApiHandler(data: string, cb: (x: string) => void): Promise<void>;
    clientAction(params: any): Promise<any>;
    accessToken(): Promise<string>;
}
