import { ClientActionData } from './entities';
export declare class GeoLocation {
    private options;
    constructor(options: GeoLocationOptions);
    capture(): Promise<GeoLocationResult>;
}
export interface GeoLocationOptions {
    accuracy: 'High' | 'Medium' | 'Low';
    maxWaitTime: number;
}
export interface GeoLocationResult {
    longitude: string;
    latitude: string;
    accuracy: number;
}
export interface GeoLocationClientResult {
    Success: boolean;
    Longitude: string;
    Latitude: string;
    Accuracy: number;
    ErrorMessage: string;
}
export interface GeoLocationData extends ClientActionData {
    Data: {
        Accuracy: string;
        MaxWaitTime: number;
    };
}
