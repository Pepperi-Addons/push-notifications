import { Item, Transaction, TransactionLine } from './data-object';
export declare class TransactionScope {
    /**
     * @param {transaction} to get transaction scope for
     * @returns Transaction scope object.
     * unlike the transaction scope on the {@link Transaction} obj,
     * this function will load the transaction scope, and not return undefined if it's not loaded.
     */
    static Get(transaction: Transaction): Promise<TransactionScope>;
    /**
     * A list of all the lines in the Transaction Scope
     */
    getLines(): Promise<TransactionLine[]>;
    /**
     * Retrieve a line from the Transaction Scope using the corresponding Item
     * @param {Item} to retrieve.
     * @returns Returns undefined when the item does not exist on the transaction scope.
     */
    getLine(item: Item): Promise<TransactionLine | undefined>;
}
