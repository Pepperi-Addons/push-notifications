import { Workflow } from '../wrappers';
import { WorkflowAction } from './actions';
import { DataObject } from '../app/entities';
export declare class WorkflowService {
    private dataObject;
    workflow: Workflow;
    constructor(dataObject: DataObject);
    performTransition(): Promise<void>;
    startProgram(name: any): Promise<void>;
    /**
     * Generate a workflow action from the wrapper object
     * @param { { actionId: string, type: string } } wAction
     * @returns {WorkflowAction}
     */
    generateAction(wAction: any): WorkflowAction | undefined;
}
