import { JSONFilter } from './index';
import { JSONBaseFilter } from './json-filter';
export declare type NodeTransformer = ((node: JSONBaseFilter) => boolean | undefined | void) | false;
export declare class JSONFilterTransformer {
    private transformers;
    constructor(transformers: {
        [key: string]: NodeTransformer;
    });
    transform(filter: JSONFilter): JSONFilter | undefined;
    private transformNode;
    private transformExpression;
    private cleanNode;
}
