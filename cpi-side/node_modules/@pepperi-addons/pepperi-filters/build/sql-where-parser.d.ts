import { JSONFilter, FieldType } from './json-filter';
export declare class SQLWhereParser {
    private fields;
    constructor(fields: {
        [key: string]: FieldType;
    });
    /**
     * Parse a SQL where clause into a JSON Filter
     * @param where An SQL style WHERE clause
     */
    parse(where: string): JSONFilter;
    private parseNode;
    private parseExpression;
    private parseBoolOperation;
    private parseBasicExpression;
    private parseNumberOperation;
    private parseStringOperation;
    private parseDateOperation;
    private parseValues;
}
