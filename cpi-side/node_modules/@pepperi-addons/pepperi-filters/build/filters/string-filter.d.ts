import Filter from './filter';
import { StringOperation } from '../json-filter';
import { Query } from 'elastic-builder';
export declare class StringFilter extends Filter {
    private operation;
    private filterValues;
    private caseSensitive;
    constructor(apiName: string, operation: StringOperation, filterValues: string[], caseSensitive?: boolean);
    apply(value: any): boolean;
    toSQLWhereClause(): string;
    private compare;
    toKibanaFilter(): Query;
}
