import { Query } from 'elastic-builder';
import Filter from './filter';
export default class FilterCollection extends Filter {
    private useAndOperation;
    private filters;
    constructor(useAndOperation?: boolean, filters?: Filter[]);
    addFilter(filter: Filter): void;
    apply(): boolean;
    filter(object: any): boolean;
    toSQLWhereClause(): string;
    toKibanaFilter(): Query;
}
